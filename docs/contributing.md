# Contributor and agent workflow

Start with [AGENTS.md](../AGENTS.md), then read the active task in
[`build-protocol/tasks`](../build-protocol/tasks). Runtime behavior follows the immutable
vendored `spine/options.proto`; do not edit it. Use generated schemas in tests and examples,
record red/green evidence, and run `npm run verify` before claiming completion.
