version: v2
modules:
  - path: proto
lint:
  use:
    # These modules compile immutable legacy Spine Proto sources whose upstream
    # naming predates the current STANDARD rules.
    - MINIMAL
  ignore_only:
    PACKAGE_DEFINED:
      - proto/spine/options.proto
breaking:
  use:
    - FILE
